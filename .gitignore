*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# 密钥库文件（重要：不要提交到Git）
keystore.properties
*.jks
*.keystore
keystore/
keystore-base64.txt

# 构建产物（Release 相关文件不应提交）
app/release/*.apk
app/release/baselineProfiles/
app/release/output-metadata.json
*.apk
*.aab

# 注意：gradle-wrapper.jar 必须提交到 Git，因为它是 Gradle Wrapper 的核心文件
